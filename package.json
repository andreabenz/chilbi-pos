{
  "name": "ceviwie-chilbi",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev:butler": "concurrently \"npm run dev:shared\" \"npm run start -w ceviwie-chilbi-butler\"",
    "dev:pos": "concurrently \"npm run dev:shared\" \"npm run start -w ceviwie-chilbi-pos\"",
    "dev:shared": "npm run dev -w @ceviwie/chilbi-shared",
    "build:butler": "npm run build:shared && npm run make -w ceviwie-chilbi-butler",
    "build:pos": "npm run build:shared && npm run make -w ceviwie-chilbi-pos",
    "build:all": "npm run build:shared && npm run make --workspaces --if-present",
    "build:shared": "npm run build -w @ceviwie/chilbi-shared",
    "lint": "eslint --fix packages/*"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5",
    "concurrently": "^9.2.1",
    "electron": "^39.2.0",
    "prettier": "^3.6.2",
    "typescript": "~5.1.0"
  }
}
